[pytest]
addopts = -s --cov=check_product_availability_lambda --cov-report=html --strict-markers
testpaths =
    test
env =
    ORDER_DATA_GETTER = MongoDb
    PAYMENT_SYSTEM_DATABASE_NAME = payment_system
    DATABASE_CLUSTER_DOMAIN = localhost